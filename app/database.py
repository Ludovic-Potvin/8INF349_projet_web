import sqlite3
from flask import current_app

def _create_db():
    # todo: if the db doesn't exist, create it and call populate
    pass

def _populate_db():
    # todo: this need to contain the api fetch and file the db
    pass
